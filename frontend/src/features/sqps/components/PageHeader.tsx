export function PageHeader({ eyebrow, title, description, action }: { eyebrow?:string; title:string; description:string; action?:React.ReactNode }) { const showEyebrow = eyebrow && eyebrow !== 'User workspace'; return <div className="mb-7 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div>{showEyebrow && <p className="eyebrow">{eyebrow}</p>}<h1 className={`page-title ${showEyebrow ? 'mt-2' : ''}`}>{title}</h1><p className="mt-2 max-w-2xl text-slate-600">{description}</p></div>{action}</div> }
