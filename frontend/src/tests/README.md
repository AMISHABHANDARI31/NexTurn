# NexTurn test strategy

Add colocated unit tests for schemas and components, integration tests for API hooks, and browser tests for each role's critical path. Mock handlers in `features/sqps/api/mock.ts` keep development deterministic.
